/**
 * Как клавиатура ведёт себя при скролле. Берём `on-drag`: она прячется, как
 * только палец пошёл, и не важно, в какой части экрана. Соседний режим
 * `interactive` на iOS выглядит эффектнее, но там клавиатура уезжает, только
 * если тянуть по самой клавиатуре — листаешь список, а она стоит.
 */
export const keyboardScroll = {
  keyboardDismissMode: 'on-drag' as const,
  // Нажатие по подсказке срабатывает сразу, а не тратится на закрытие клавиатуры
  keyboardShouldPersistTaps: 'handled' as const,
};
